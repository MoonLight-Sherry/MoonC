#include <iostream>

using namespace std;

int main0()
{
	int n, m, kase = 0;

	while (scanf_s("%d%d", &n, &m) == 2)
	{
		for (int i = 0; i <= n; i++)
		{
			//left[i] = i - 1;
			//right[i] = (i + 1) % (n + 1);
		}
	}
	return 0;
}